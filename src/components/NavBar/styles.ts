import styled from 'styled-components'

const NavBar = styled.nav`
  margin-bottom: 50px;
`

const UL = styled.ul`
  list-style: none;
  display: flex;
  flex-direction: row;
`

const LI = styled.li`
  padding-right: 5px;
  margin-right: 5px;
`

export { NavBar, UL, LI }
